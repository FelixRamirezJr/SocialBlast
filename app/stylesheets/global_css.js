'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  container: {
    padding: 10,
    marginTop: 20
  },
  boxed: {
    padding: 10,
    backgroundColor: 'white',
    borderRadius: 2,
    borderColor: 'black'
  }
});
